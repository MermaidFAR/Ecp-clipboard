# 验证记录（2026-09-25）

此版本仍在验收。指标针对 Windows Release 构建；现有测量没有达到弹窗延迟目标，也没有完成后台空闲占用及 Windows 接管的隔离验证，因此不作为已完成的发布版本。

## 已完成的自动检查

- `cargo fmt --all -- --check`、`cargo clippy --workspace --all-targets --locked -- -D warnings` 和 `cargo test --workspace --locked`。
- 源码提交 `7fd145e` 的 [Windows CI](https://github.com/MermaidFAR/Ecp-clipboard/actions/runs/36152092147) 已通过格式、Clippy、测试及三个 Release 程序的构建检查。
- 单元测试：后台与共享库 21 项、GPUI 输入 1 项，全部通过。三项会修改剪贴板的 Windows 集成测试有意标为 `ignored`，须在隔离的交互式会话中执行。
- GPUI Release 程序和独立后台/CLI Release 程序分别构建成功。
- 在 2000 条隔离样本库中，Release CLI 的 `search '测试文字 1799'` 返回稳定 `id=1800`，`list 2` 返回最近的 `id=2000`、`id=1999`。此检查只读历史，没有改动用户剪贴板。
- 在隔离的临时配置与数据库目录中，GPUI 第二次启动会激活已有实例；发出隐藏消息后，窗口进程退出。
- 旧版实库升级演练使用 SQLite 一致性备份副本：版本 0 升到 3 后仍有 5117 条，`quick_check=ok`，原 `max_history=200` 自动升到 5117，以免旧版仅用于显示的上限在升级时删除数据。

## 本机切换（用户明确授权）

旧后台已停止，三个新版程序安装在 `%LOCALAPPDATA%\Programs\EcpClipboard`，开机启动项已改指向新版。切换前备份包含 5117 条历史的 SQLite 快照、旧配置、旧程序和注册表相关值；快照 `quick_check=ok`。旧版遗留 `DisabledHotkeys=V` 且没有原值备份，用户单独授权移除这个 `V` 后重新接管。新版状态显示“Win+V 已接管；Ctrl+Alt+V 可用”；实库升至版本 3 后有 5118 条，`quick_check=ok`，旧图缩略图迁移完成。安装版 GPUI 窗口已启动到可操作状态，工作集快照约 71.7 MB。第二次启动后台会立即退出，原实例继续运行。

这些是当前配置下的一次实际运行检查，不能代替下面的隔离失败注入测试或延迟统计。随后用户主动清空了当前历史库，程序按其选择保留空库；完整备份未删除。清空后执行 SQLite `VACUUM` 和 WAL 截断，活动数据库缩至 20,480 字节，`quick_check=ok`。旧版曾把 `EnableClipboardHistory` 和 `EnableCloudClipboard` 设为 0；由于旧版没有保存两者原值，本次没有推测或更改它们。

交互修复后，安装版连续触发两次 Win+V：第一次出现 `ecp-ui.exe`，第二次该进程退出，后台保持运行。另在隔离配置下启动窗口并发送 Alt+Tab：前台焦点转到其他程序后，GPUI 窗口进程退出。修复版重新构建并安装，热键状态仍显示“Win+V 已接管；Ctrl+Alt+V 可用”。

## 界面启动基准

测试机器：Windows 11 专业版 10.0.26200，AMD Ryzen AI 9 HX 370，Rust 1.95.0。`cargo run -p ecp-clipboard --release --example bench_ui -- <dataset> 10` 为每组创建独立临时数据库并启动十次 `ecp-ui.exe`。时间从创建进程到首屏历史加载完成并绘制下一帧的标记；第一轮单列，余下九轮取 p50/p95。峰值工作集由 Windows `GetProcessMemoryInfo` 读取；表中为该组观测到的约数。图片样本是 256×256 的独立 RGBA 像素。测量未触发实际热键，故是“启动到可操作”的代理数据，实际热键延迟仍待测。第一轮只是该数据集的首次运行，**不是清空系统缓存后的真正冷启**。

| 数据集 | 首次运行 | 后续 p50 | 后续 p95 | 界面峰值工作集 | 数据目录 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 空库 | 652.0 ms | 358.2 ms | 400.1 ms | 约 70.4 MB | 0 MB |
| 200 条文本 | 424.8 ms | 386.7 ms | 431.8 ms | 约 72.2 MB | 0 MB |
| 200 张图片 | 390.7 ms | 375.1 ms | 470.0 ms | 约 81.5 MB | 68.4 MB |
| 2000 条混合记录 | 360.4 ms | 368.6 ms | 382.6 ms | 约 82.5 MB | 68.6 MB |

目标是实际热键弹窗 p95 ≤300 ms、后台空闲 10 分钟工作集 ≤10 MB、界面峰值 ≤100 MB。现有 UI 启动代理 p95 比 300 ms 高 82.6–170.0 ms；四组界面峰值低于 100 MB。本机安装版后台在数据库最后一次写入后 10.64 分钟的快照为工作集 **17.59 MB**、私有内存 6.43 MB，超过工作集目标 7.59 MB。该段末约 3.5 分钟的累计 CPU 时间仅增加约 0.02 秒，但没有完成四组负载的标准化空闲 CPU 测量。真正冷启也未测。应继续缩短 GPUI 进程和首帧启动路径、降低后台工作集，然后测实际热键全链路，不能把代理值当成达标证明。

可选 `ECP_UI_TRACE_FILE` 将 `main`、配置、数据库、GPUI application、建窗、历史加载和首帧时间写入指定文件。五次空库诊断中，数据库在进程进入 `main` 后约 7–14 ms 打开；GPUI application 回调约在 189–200 ms，建窗约在 249–276 ms，首帧约在 270–290 ms。进程创建到 `main` 的开销还需计入总延迟。这说明此样本的瓶颈主要在 GPUI/DirectX 初始化和建窗，不是首屏 SQLite 元数据查询；这个判断仅针对该机器与空库。

## 隔离 Windows 会话待验

- Win+V 首次启用、注册失败回滚、正常退出恢复、异常退出后的原值检查、重复启动、系统不允许接管时 Ctrl+Alt+V 的可用性。
- CF_DIB、CF_HDROP 文件列表、原图尺寸及透明度的系统剪贴板往返，以及搜索结果按 ID 复制。
- 四组数据下重复测后台十分钟空闲工作集和 CPU；空库、200 文本、200 图片、2000 混合数据的热键 p50/p95 与真正冷启。
- 中文输入法候选窗口、不同 DPI 和远程桌面显示。现有 UTF-16 光标单元测试不能代替交互式输入法验证。

当前机器未提供可用的隔离 Windows 会话，因此没有在用户桌面模拟注册失败、崩溃或改写剪贴板格式。上节记录的是用户授权的正常切换结果。

## 发布许可

锁定的 Zed/GPUI 提交为 `397cbc84de333eeb56849dc90782d365b37e60d7`。本机针对锁文件依赖元数据的扫描没有发现缺失许可字段或 GPL-only 包；`gpui`、`ztracing` 等标记为 Apache-2.0，另有 MPL-2.0 依赖。元数据扫描不能替代对实际许可证文本、声明和分发方式的核对。[上游传递许可报告](https://github.com/zed-industries/zed/issues/55470)仍应作为审阅线索；当前锁定提交的元数据与报告提出时可能不同。发布工作流只构建候选，不上传二进制，待人工核对完成后再决定公开发布。
